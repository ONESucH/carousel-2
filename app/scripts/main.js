'use strict';

var allImages = document.getElementsByClassName('all-images');

function carousel(valueButton) {
    var indent = 400;

    switch (valueButton) {
        case 'leftButton' :
            break;
        case 'rightButton' :
            break;
    }
}